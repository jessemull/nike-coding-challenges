{
  "name": "nike-pdm-test",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "execute": "node -e 'require(\"./src/main.js\").run()'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}
